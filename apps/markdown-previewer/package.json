{
  "name": "markdown-previewer",
  "version": "0.0.1",
  "description": "Responsive Web Design project for freeCodeCamp",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "parcel build --out-dir=dist",
    "test": "wait-port -t 10000 3000"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "parcel": "^2.7.0"
  }
}
